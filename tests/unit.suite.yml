 # Codeception Test Suite Configuration
#
# Suite for unit or integration tests.

actor: UnitTester
modules:
    enabled:
        - Yii2:
            configFile: 'tests/config.php'
            part: orm
            transaction: false
        - Asserts
        - \Helper\Unit